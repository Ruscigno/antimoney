# Terraform state — contains generated secrets, never commit
terraform.tfstate
terraform.tfstate.backup
.terraform/
.terraform.lock.hcl

# Variable files with real values — never commit
terraform.tfvars
*.auto.tfvars

# Crash logs
crash.log
crash.*.log
